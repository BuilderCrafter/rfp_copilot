# Frontend

React + TypeScript + Vite frontend for Serbian Vibe RFP.

## Run

```bash
npm install
npm run dev
```

## Rules

- Use API shapes from `../openapi.yaml`.
- Keep API JSON fields in `snake_case`.
- Build the reviewer workflow first: questions, answer editor, citations, approve/flag/reject.
- Mock data is fine until backend endpoints are ready.
